# yt_transcribe

Tiny CLI: download audio from a YouTube (or any yt-dlp-supported) URL with `yt-dlp`,
then transcribe it with OpenAI's Whisper API (`whisper-1`). Requires
`OPENAI_API_KEY` in the environment.

See `AGENTS.md` for project conventions and `yt_transcribe.py --help` for usage.
